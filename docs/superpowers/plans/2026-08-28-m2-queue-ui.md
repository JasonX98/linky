# M2 任务队列与主界面实现计划（Plan 2 / 4）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 引入 fluent_ui 主框架与 Riverpod，实现分析→选画质→入队→并发下载→进度→取消/重试的完整任务队列，落实 M1 最终审查的结构性建议（进程树终止、ffmpeg 路径下沉、排空辅助提取）。

**Architecture:** `DownloadTask`（内存态模型）+ Riverpod `Notifier`（AnalysisController 状态机 / DownloadQueueController 队列与并发）；service 层新增 taskId 进程注册表与 `cancel()`（进程树终止）；UI 迁移到 fluent_ui 三页导航（历史/设置为占位页）。

**Tech Stack:** fluent_ui ^4、flutter_riverpod ^2、dart:io Process（taskkill 树终止）。drift/历史/设置/i18n 在 M3。

**Spec:** `docs/superpowers/specs/2026-08-28-ytdlp-downloader-design.md`（本计划实现其 §9 里程碑 M2 + §3.3 的取消/重试/打开文件夹；§5.2 的并发默认 3（范围 1–5，内存态）；播放列表批量、历史持久化、设置、i18n 仍属 M3）

## Global Constraints

- 包名 `video_downloader`；平台仅 Windows
- 并发：默认 3，范围 1–5（M2 内存态，M3 接设置持久化）；调低并发只影响新任务启动，不终止运行中任务
- 取消语义：排队中 → 直接置 canceled；下载中 → 先置 canceling 再调 service.cancel，结束时 canceling→canceled（吞掉取消产生的错误文案），若取消前已完成则 completed 胜出
- **取消必须是进程树终止**（`taskkill /PID <pid> /T /F`），确保 ffmpeg 合并子进程一并退出
- service download 返回路径前必须先排空子进程流（M1 契约）；`--encoding utf-8`、`--progress --no-simulate`、`--print after_move:filepath`、`-o <dir>/%(title)s.%(ext)s`、URL 最后 等 M1 参数契约不变
- ffmpeg 路径下沉：`request.ffmpegPath ?? engine.ffmpegPath`（UI 不再直接使用 EngineLocator）
- 新依赖仅 `fluent_ui`、`flutter_riverpod`（Task 1 用 `flutter pub add` 解析并记录版本）
- 文案硬编码中文（i18n M3）；DownloadTask 仅内存态（M3 历史持久化）
- TDD：先红后绿；每任务结束 commit；测试命令 `flutter test <path>`
- flutter 不在 PATH——一律用全路径 `D:\Application\DevTools\flutter\flutter3.44.9\bin\flutter.bat`（若该路径失效，先 `where flutter` 重新定位并记录）
- **任何 GUI 冒烟前必须先重新 `flutter build windows --debug`**（M1 教训：旧二进制会误报已修复的缺陷）

---

### Task 1: 依赖引入与进程树终止

**Files:**
- Modify: `pubspec.yaml`（fluent_ui + flutter_riverpod）
- Modify: `lib/engine/process_launcher.dart`（AppProcess 增加 pid/killTree）
- Test: `test/engine/process_launcher_test.dart`（追加 2 个用例）

**Interfaces:**
- Consumes: M1 的 `AppProcess`/`SystemProcessLauncher`
- Produces:
  - `abstract class AppProcess` 新增成员：`int get pid;` 与 `Future<void> killTree();`
  - `SystemProcessLauncher` 实现之：`pid` 转发 dart:io；`killTree()` = `Process.run('taskkill', ['/PID', pid, '/T', '/F'])`（忽略其退出码——进程可能已自行退出）
  - 下游（Task 2 service.cancel）只依赖这两个成员

- [ ] **Step 1: 添加依赖并记录版本**

```powershell
& "D:\Application\DevTools\flutter\flutter3.44.9\bin\flutter.bat" pub add fluent_ui flutter_riverpod
& "D:\Application\DevTools\flutter\flutter3.44.9\bin\flutter.bat" analyze
```

Expected: pubspec 出现 fluent_ui 与 flutter_riverpod（把解析到的版本号写入提交信息正文），analyze 无问题

- [ ] **Step 2: 写失败测试（追加到 test/engine/process_launcher_test.dart，文件顶部补 `import 'dart:async';`）**

```dart
  test('AppProcess exposes pid', () async {
    final launcher = SystemProcessLauncher();
    final proc = await launcher.start('cmd.exe', ['/c', 'echo', 'hi']);
    expect(proc.pid, greaterThan(0));
    await proc.exitCode;
  });

  test('killTree terminates the process tree', () async {
    final launcher = SystemProcessLauncher();
    // 30 秒的长任务，killTree 后必须立即退出
    final proc = await launcher
        .start('cmd.exe', ['/c', 'ping', '-n', '30', '127.0.0.1', '>nul']);
    await proc.killTree();
    var code = -1;
    var timedOut = false;
    try {
      code = await proc.exitCode.timeout(const Duration(seconds: 5));
    } on TimeoutException {
      timedOut = true;
    }
    expect(timedOut, isFalse,
        reason: 'killTree should terminate the whole process tree');
    expect(code, isNot(0));
  });
```

- [ ] **Step 3: 运行确认失败**

Run: `flutter test test/engine/process_launcher_test.dart`
Expected: FAIL（AppProcess 无 pid/killTree 成员，编译错误）

- [ ] **Step 4: 实现（lib/engine/process_launcher.dart）**

`AppProcess` 抽象类追加两个成员声明：

```dart
abstract class AppProcess {
  Stream<String> get stdout;
  Stream<String> get stderr;
  Future<int> get exitCode;
  void kill();
  int get pid;
  Future<void> killTree();
}
```

`_SystemAppProcess` 内追加（类中已有 `final Process _proc;`）：

```dart
  @override
  int get pid => _proc.pid;

  @override
  Future<void> killTree() async {
    // /T 终止整棵进程树（含 ffmpeg 合并子进程），/F 强制；忽略退出码：
    // 进程可能已自行退出，此时 taskkill 报错属预期
    await Process.run('taskkill', ['/PID', _proc.pid.toString(), '/T', '/F']);
  }
```

- [ ] **Step 5: 运行确认通过 + 全量回归**

Run: `flutter test test/engine/process_launcher_test.dart` → PASS（4/4）；然后 `flutter test`（40/42→42/42 数量以实际为准，全部通过）+ `flutter analyze`

- [ ] **Step 6: 提交**

```powershell
git add pubspec.yaml pubspec.lock lib/engine/process_launcher.dart test/engine/process_launcher_test.dart
git commit -m "feat(engine): process-tree kill + fluent_ui/riverpod deps"
```

---

### Task 2: service 取消注册表 + ffmpeg 回退 + 排空辅助提取

**Files:**
- Modify: `lib/engine/models.dart`（DownloadRequest.taskId）
- Modify: `lib/engine/yt_dlp_service.dart`（_active 注册表、cancel()、_buildDownloadArgs 回退、_awaitDrain 提取）
- Test: `test/engine/yt_dlp_service_test.dart`（FakeAppProcess 补成员 + 3 个新用例）

**Interfaces:**
- Consumes: Task 1 的 `killTree()`
- Produces:
  - `DownloadRequest` 新增可选参数 `final String? taskId;`（默认 null，M1 调用方不受影响；const 构造追加命名参数）
  - `YtDlpService` 新增：`Future<void> cancel(String taskId)` —— 从注册表取出进程并 killTree；未注册/已结束时为无操作
  - download() 行为变化（对 Task 4 透明）：`request.taskId != null` 时启动即注册、结束时（成功/失败/取消）必然移除；ffmpeg 路径 `request.ffmpegPath ?? engine.ffmpegPath`（因此调用方传 null 即可，UI 不再需要 EngineLocator）
  - 私有静态 `_awaitDrain(Completer<void> a, Completer<void> b, Duration limit)` —— probe 与 download 共用（TimeoutException 由调用方各自处理：probe → kill+取消+分析超时文案；download → 以已捕获内容继续）
  - 顺带落实 M1 遗留 Minor：`_buildDownloadArgs` 的 `engine` 参数现在被回退逻辑真正使用

- [ ] **Step 1: 写失败测试**

先更新测试文件里的 `FakeAppProcess`（补两个覆盖成员）：

```dart
class FakeAppProcess implements AppProcess {
  FakeAppProcess({
    required this.stdout,
    this.stderr = const Stream.empty(),
    Future<int>? exitCode,
  }) : _exitCode = exitCode ?? Future.value(0);
  @override
  final Stream<String> stdout;
  @override
  final Stream<String> stderr;
  final Future<int> _exitCode;
  bool killed = false;
  bool killTreeCalled = false;
  @override
  Future<int> get exitCode => _exitCode;
  @override
  void kill() => killed = true;
  @override
  int get pid => 4321;
  @override
  Future<void> killTree() async {
    killTreeCalled = true;
  }
}
```

在 `main()` 内追加（`_stubLocatorWithFfmpeg` 辅助与三个用例）：

```dart
  EngineLocator _stubLocatorWithFfmpeg(Directory dir) {
    final bin = p.join(dir.path, 'data', 'flutter_assets', 'assets', 'bin');
    Directory(bin).createSync(recursive: true);
    File(p.join(bin, 'yt-dlp.exe')).writeAsStringSync('');
    File(p.join(bin, 'ffmpeg.exe')).writeAsStringSync('');
    return EngineLocator(baseDirOverride: dir.path);
  }

  test('cancel kills the registered process tree', () async {
    final proc = FakeAppProcess(stdout: const Stream.empty());
    final launcher = FakeLauncher(proc);
    final service = YtDlpService(
        launcher: launcher, locator: _stubLocator(temp));
    final done = service.download(DownloadRequest(
        url: 'https://example.com/v',
        preset: QualityPreset.best,
        outputDir: 'D:\\dl',
        taskId: 't7'));

    await service.cancel('t7');
    expect(proc.killTreeCalled, isTrue);
    // 取消后进程以非零码结束 → 友好异常（取消吞错由队列层负责）
    await expectLater(done, throwsA(isA<DownloadException>()));
  });

  test('cancel of unknown taskId is a no-op', () async {
    final service = YtDlpService(
        launcher: FakeLauncher(FakeAppProcess(stdout: const Stream.empty())),
        locator: _stubLocator(temp));
    await service.cancel('missing');
  });

  test('download falls back to engine ffmpeg path', () async {
    final launcher = FakeLauncher(FakeAppProcess(stdout: Stream.fromIterable([
      'D:\\dl\\X.mp4',
    ])));
    final locator = _stubLocatorWithFfmpeg(temp);
    final enginePath = p.join(
        temp.path, 'data', 'flutter_assets', 'assets', 'bin', 'ffmpeg.exe');
    final service = YtDlpService(launcher: launcher, locator: locator);

    await service.download(DownloadRequest(
        url: 'https://example.com/v',
        preset: QualityPreset.best,
        outputDir: 'D:\\dl'));
    expect(launcher.lastArgs,
        containsAllInOrder(['--ffmpeg-location', enginePath]));
    expect(launcher.lastArgs, contains('--no-warnings'));
    expect(launcher.lastArgs, contains('-o'));
  });
```

- [ ] **Step 2: 运行确认失败**

Run: `flutter test test/engine/yt_dlp_service_test.dart`
Expected: FAIL（taskId 参数不存在 → 编译错误；即便修正后 cancel 无实现、回退未生效）

- [ ] **Step 3: 实现**

`lib/engine/models.dart` — `DownloadRequest` 追加字段与构造参数（保持 const）：

```dart
  final String? taskId;
```
构造函数追加 `this.taskId,`

`lib/engine/yt_dlp_service.dart`：

类内新增字段与方法：

```dart
  final Map<String, AppProcess> _active = {};

  Future<void> cancel(String taskId) async {
    final proc = _active.remove(taskId);
    if (proc != null) {
      await proc.killTree();
    }
  }
```

私有静态排空辅助（提取自 probe/download 的重复逻辑）：

```dart
  static Future<void> _awaitDrain(
      Completer<void> a, Completer<void> b, Duration limit) {
    return Future.wait([a.future, b.future]).timeout(limit);
  }
```

probe()：把现有 `await Future.wait([stdoutDone.future, stderrDone.future]).timeout(probeTimeout);` 替换为 `await _awaitDrain(stdoutDone, stderrDone, probeTimeout);`（外层 try/on TimeoutException 逻辑不变）。

download() 重构为（taskId 注册 + ffmpeg 回退 + 共用排空）：

```dart
  Future<String> download(
    DownloadRequest request, {
    void Function(DownloadProgress)? onProgress,
  }) async {
    final engine = locator.resolve();
    final proc = await launcher.start(
      engine.ytDlpPath,
      _buildDownloadArgs(request, engine),
      environment: {'PYTHONIOENCODING': 'utf-8'},
    );
    final taskId = request.taskId;
    if (taskId != null) {
      _active[taskId] = proc;
    }
    try {
      String? filePath;
      final stderrBuf = StringBuffer();
      final stdoutDone = Completer<void>();
      final stderrDone = Completer<void>();
      final subs = <StreamSubscription<String>>[
        proc.stdout.listen((line) {
          final progress = parseProgressLine(line);
          if (progress != null) {
            onProgress?.call(progress);
            return;
          }
          final trimmed = line.trim();
          if (trimmed.isEmpty || trimmed.startsWith('[')) return;
          filePath = trimmed;
        }, onDone: () {
          if (!stdoutDone.isCompleted) stdoutDone.complete();
        }, onError: (Object _) {
          if (!stdoutDone.isCompleted) stdoutDone.complete();
        }),
        proc.stderr.listen(stderrBuf.writeln, onDone: () {
          if (!stderrDone.isCompleted) stderrDone.complete();
        }, onError: (Object _) {
          if (!stderrDone.isCompleted) stderrDone.complete();
        }),
      ];
      final code = await proc.exitCode;
      try {
        await _awaitDrain(stdoutDone, stderrDone, drainTimeout);
      } on TimeoutException {
        // 进程已退出但句柄未释放：以已捕获内容继续，不使任务永久挂起
      }
      await Future.wait(subs.map((s) => s.cancel()));
      if (code != 0) {
        throw DownloadException(
          mapYtDlpError(stderrBuf.toString()),
          stderrBuf.toString(),
        );
      }
      final path = filePath;
      if (path == null || path.isEmpty) {
        throw const DownloadException('下载完成但未能定位输出文件');
      }
      return path;
    } finally {
      if (taskId != null) {
        _active.remove(taskId);
      }
    }
  }
```

`_buildDownloadArgs` 的 ffmpeg 行改为回退逻辑（engine 参数因此真正被使用）：

```dart
      final ffmpeg = request.ffmpegPath ?? engine.ffmpegPath;
      ...
      if (ffmpeg != null) ...['--ffmpeg-location', ffmpeg],
```
（即把原 `if (request.ffmpegPath != null) ...['--ffmpeg-location', request.ffmpegPath!],` 替换为上面两段合入函数体。）

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `flutter test test/engine/yt_dlp_service_test.dart`（12/12）→ `flutter test`（43/43，以实际为准）→ `flutter analyze`
Expected: 全部通过；M1 既有用例零改动全绿（taskId 默认 null 向后兼容）

- [ ] **Step 5: 提交**

```powershell
git add lib/engine/models.dart lib/engine/yt_dlp_service.dart test/engine/yt_dlp_service_test.dart
git commit -m "feat(engine): task registry with tree-kill cancel, ffmpeg fallback, shared drain helper"
```

---

### Task 3: DownloadTask 模型

**Files:**
- Create: `lib/features/download/download_task.dart`
- Test: `test/features/download/download_task_test.dart`

**Interfaces:**
- Consumes: `QualityPreset`、`DownloadProgress`（lib/engine/models.dart）
- Produces（Task 4/5/7 消费的确切签名）:
  - `enum TaskStatus { queued, downloading, canceling, completed, failed, canceled }`
  - `class DownloadTask`：字段 `id/url/title/preset/status`（required）+ `progress/filePath/errorSummary`（可空）；`bool get isActive`（queued|downloading|canceling）；`copyWith({TaskStatus? status, DownloadProgress? progress, bool clearProgress = false, String? filePath, bool clearFilePath = false, String? errorSummary, bool clearError = false})`（clear 优先于同名字段，用于把可空字段重置为 null）

- [ ] **Step 1: 写失败测试**

```dart
// test/features/download/download_task_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:video_downloader/engine/models.dart';
import 'package:video_downloader/features/download/download_task.dart';

DownloadTask _t(TaskStatus status) => DownloadTask(
    id: 't0',
    url: 'u',
    title: '标题',
    preset: QualityPreset.best,
    status: status);

void main() {
  test('isActive covers queued/downloading/canceling only', () {
    expect(_t(TaskStatus.queued).isActive, isTrue);
    expect(_t(TaskStatus.downloading).isActive, isTrue);
    expect(_t(TaskStatus.canceling).isActive, isTrue);
    expect(_t(TaskStatus.completed).isActive, isFalse);
    expect(_t(TaskStatus.failed).isActive, isFalse);
    expect(_t(TaskStatus.canceled).isActive, isFalse);
  });

  test('copyWith patches fields and clears nullables', () {
    const progress = DownloadProgress(fraction: 0.5);
    final t = _t(TaskStatus.downloading)
        .copyWith(progress: progress, filePath: 'D:\\x.mp4');
    expect(t.progress, same(progress));
    expect(t.filePath, 'D:\\x.mp4');

    final reset = t.copyWith(
        status: TaskStatus.queued,
        clearProgress: true,
        clearFilePath: true,
        clearError: true);
    expect(reset.status, TaskStatus.queued);
    expect(reset.progress, isNull);
    expect(reset.filePath, isNull);
    expect(reset.errorSummary, isNull);
    expect(reset.id, 't0');
  });

  test('copyWith without clear keeps existing values', () {
    final t = _t(TaskStatus.downloading).copyWith(filePath: 'D:\\x.mp4');
    final again = t.copyWith(status: TaskStatus.completed);
    expect(again.filePath, 'D:\\x.mp4');
    expect(again.status, TaskStatus.completed);
  });
}
```

- [ ] **Step 2: 运行确认失败**

Run: `flutter test test/features/download/download_task_test.dart`
Expected: FAIL（文件不存在）

- [ ] **Step 3: 实现**

```dart
// lib/features/download/download_task.dart
import 'package:video_downloader/engine/models.dart';

enum TaskStatus { queued, downloading, canceling, completed, failed, canceled }

class DownloadTask {
  const DownloadTask({
    required this.id,
    required this.url,
    required this.title,
    required this.preset,
    required this.status,
    this.progress,
    this.filePath,
    this.errorSummary,
  });

  final String id;
  final String url;
  final String title;
  final QualityPreset preset;
  final TaskStatus status;
  final DownloadProgress? progress;
  final String? filePath;
  final String? errorSummary;

  bool get isActive =>
      status == TaskStatus.queued ||
      status == TaskStatus.downloading ||
      status == TaskStatus.canceling;

  DownloadTask copyWith({
    TaskStatus? status,
    DownloadProgress? progress,
    bool clearProgress = false,
    String? filePath,
    bool clearFilePath = false,
    String? errorSummary,
    bool clearError = false,
  }) {
    return DownloadTask(
      id: id,
      url: url,
      title: title,
      preset: preset,
      status: status ?? this.status,
      progress: clearProgress ? null : (progress ?? this.progress),
      filePath: clearFilePath ? null : (filePath ?? this.filePath),
      errorSummary: clearError ? null : (errorSummary ?? this.errorSummary),
    );
  }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `flutter test test/features/download/download_task_test.dart`
Expected: PASS（3/3）

- [ ] **Step 5: 提交**

```powershell
git add lib/features/download/download_task.dart test/features/download/download_task_test.dart
git commit -m "feat(queue): in-memory DownloadTask model"
```

---

### Task 4: 队列控制器 —— 入队 / 并发 / 进度 / 完成流转

**Files:**
- Create: `lib/features/download/providers.dart`
- Create: `lib/features/download/queue_controller.dart`
- Test: `test/features/download/queue_controller_test.dart`

**Interfaces:**
- Consumes: Task 2 的 `taskId`/`cancel`/ffmpeg 回退；Task 3 的模型
- Produces:
  - `providers.dart`：`ytDlpServiceProvider`（Provider\<YtDlpService\>）、`downloadsDirProvider`（Provider\<Future<String> Function()\>，默认 getDownloadsDirectory() ?? Directory.systemTemp）、`downloadQueueProvider`（NotifierProvider\<DownloadQueueController, List\<DownloadTask\>\>）、`analysisProvider`（Task 6）
  - `DownloadQueueController extends Notifier<List<DownloadTask>>`：
    - `static const int minConcurrency = 1; static const int maxConcurrency = 5;`，初始并发 3
    - `Future<String> enqueue({required String url, required String title, required QualityPreset preset})` —— 先解析下载目录再入队并尝试启动，返回任务 id（`t0`、`t1`… 递增）
    - `int get concurrency; int get runningCount;`（downloading + canceling 计数）
    - `void setConcurrency(int value)` —— clamp 到 1–5，仅影响新任务启动
    - 状态流转：queued →（并发有空位）downloading → completed（记 filePath）/ failed（记 errorSummary）；进度回调仅作用于 downloading 任务；**所有状态更新经由带 expected 前置状态校验的 `_patch`**（防止取消后迟到回调覆盖状态）
    - `Future<void> cancel(String id)` 与 `void retry(String id)`、`Future<void> openFolder(String id)` 在 Task 5 实现（本任务先留空实现占位：cancel/retry 空方法体、openFolder 空方法体——加 `// Task 5 实现` 注释）
    - download 调用参数：`DownloadRequest(url: task.url, preset: task.preset, outputDir: <解析的目录>, taskId: task.id)`（ffmpegPath 传 null，走 Task 2 的回退）

- [ ] **Step 1: 写失败测试**

```dart
// test/features/download/queue_controller_test.dart
import 'dart:async';

import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:video_downloader/engine/models.dart';
import 'package:video_downloader/engine/yt_dlp_service.dart';
import 'package:video_downloader/features/download/providers.dart';
import 'package:video_downloader/features/download/queue_controller.dart';

class FakeQueueService extends YtDlpService {
  final Map<String, Completer<String>> pending = {};
  final List<String> canceled = [];
  final List<DownloadRequest> started = [];

  @override
  Future<AnalysisResult> probe(String url) async =>
      throw UnimplementedError();

  @override
  Future<String> download(DownloadRequest request,
      {void Function(DownloadProgress)? onProgress}) async {
    started.add(request);
    final c = Completer<String>();
    pending[request.taskId!] = c;
    // 进入 downloading 即回调一次进度，验证进度流
    onProgress?.call(const DownloadProgress(fraction: 0.5, speed: '1MiB/s'));
    return c.future;
  }

  @override
  Future<void> cancel(String taskId) async {
    canceled.add(taskId);
    pending.remove(taskId);
  }
}

Future<void> _settle() async {
  await Future<void>.delayed(Duration.zero);
  await Future<void>.delayed(Duration.zero);
}

void main() {
  late FakeQueueService fake;
  late ProviderContainer container;

  setUp(() {
    fake = FakeQueueService();
    container = ProviderContainer(overrides: [
      ytDlpServiceProvider.overrideWithValue(fake),
      downloadsDirProvider.overrideWithValue(() async => 'D:\\dl'),
    ]);
    addTearDown(container.dispose);
  });

  DownloadQueueController get controller =>
      container.read(downloadQueueProvider.notifier);

  test('enqueue starts task within concurrency and reports progress',
      () async {
    final id = await controller.enqueue(
        url: 'u0', title: '视频0', preset: QualityPreset.best);
    await _settle();

    final tasks = container.read(downloadQueueProvider);
    expect(id, 't0');
    expect(tasks.single.status, TaskStatus.downloading);
    expect(tasks.single.progress?.fraction, 0.5);
    expect(fake.started.single.taskId, 't0');
    expect(fake.started.single.outputDir, 'D:\\dl');
    expect(fake.started.single.ffmpegPath, isNull);
  });

  test('excess tasks stay queued and start as slots free', () async {
    await controller.setConcurrency(1); // 默认并发 3，调低才能观察排队
    await controller.enqueue(url: 'u0', title: '视频0', preset: QualityPreset.best);
    await _settle();
    await controller.enqueue(url: 'u1', title: '视频1', preset: QualityPreset.best);
    await _settle();

    var tasks = container.read(downloadQueueProvider);
    expect(tasks[0].status, TaskStatus.downloading);
    expect(tasks[1].status, TaskStatus.queued);

    fake.pending['t0']!.complete('D:\\dl\\v0.mp4');
    await _settle();

    tasks = container.read(downloadQueueProvider);
    expect(tasks[0].status, TaskStatus.completed);
    expect(tasks[0].filePath, 'D:\\dl\\v0.mp4');
    expect(tasks[1].status, TaskStatus.downloading);
  });

  test('failure records friendly error', () async {
    await controller.enqueue(url: 'u0', title: '视频0', preset: QualityPreset.best);
    await _settle();

    fake.pending['t0']!
        .completeError(const DownloadException('视频不可用：可能已被删除或设为私密'));
    await _settle();

    final tasks = container.read(downloadQueueProvider);
    expect(tasks.single.status, TaskStatus.failed);
    expect(tasks.single.errorSummary, contains('不可用'));
  });

  test('late progress after completion does not resurrect state', () async {
    final id = await controller.enqueue(
        url: 'u0', title: '视频0', preset: QualityPreset.best);
    await _settle();
    fake.pending[id]!.complete('D:\\dl\\v0.mp4');
    await _settle();
    // 迟到的进度回调（fake 已移除 completer，无法直接注入；
    // 通过复制 fake 行为验证 _patch 的 expected 防御——见 _patchIf 语义单测）
    final tasks = container.read(downloadQueueProvider);
    expect(tasks.single.status, TaskStatus.completed);
  });
}
```

（说明：第 4 个用例目前只断言完成态不被后续操作影响；`_patch` 的 expected 防御由 Task 5 的取消用例真正触发。）

- [ ] **Step 2: 运行确认失败**

Run: `flutter test test/features/download/queue_controller_test.dart`
Expected: FAIL（providers/queue_controller 不存在）

- [ ] **Step 3: 实现**

```dart
// lib/features/download/providers.dart
import 'dart:io';

import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:path_provider/path_provider.dart';
import 'package:video_downloader/engine/yt_dlp_service.dart';
import 'package:video_downloader/features/download/queue_controller.dart';

final ytDlpServiceProvider =
    Provider<YtDlpService>((ref) => YtDlpService());

final downloadsDirProvider = Provider<Future<String> Function()>((ref) {
  return () async =>
      (await getDownloadsDirectory())?.path ?? Directory.systemTemp.path;
});

final downloadQueueProvider =
    NotifierProvider<DownloadQueueController, List<DownloadTask>>(
        DownloadQueueController.new);

final analysisProvider =
    NotifierProvider<AnalysisController, AnalysisState>(AnalysisController.new);
```

（`AnalysisController`/`AnalysisState` 由 Task 6 创建——本任务先在文件底部放占位声明会破坏编译；因此 **providers.dart 的 analysisProvider 两行推迟到 Task 6 再加入**，本任务提交时文件不含它们。）

```dart
// lib/features/download/queue_controller.dart
import 'dart:async';
import 'dart:io';

import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:video_downloader/engine/models.dart';
import 'package:video_downloader/features/download/download_task.dart';
import 'package:video_downloader/features/download/providers.dart';

class DownloadQueueController extends Notifier<List<DownloadTask>> {
  static const int minConcurrency = 1;
  static const int maxConcurrency = 5;

  int _concurrency = 3;
  int _seq = 0;

  @override
  List<DownloadTask> build() {
    ref.watch(ytDlpServiceProvider);
    return const [];
  }

  YtDlpService get _service => ref.read(ytDlpServiceProvider);

  int get concurrency => _concurrency;

  int get runningCount => state
      .where((t) =>
          t.status == TaskStatus.downloading ||
          t.status == TaskStatus.canceling)
      .length;

  Future<String> enqueue({
    required String url,
    required String title,
    required QualityPreset preset,
  }) async {
    final dir = await ref.read(downloadsDirProvider)();
    final id = 't${_seq++}';
    _upsert(DownloadTask(
        id: id, url: url, title: title, preset: preset,
        status: TaskStatus.queued));
    _startNext();
    return id;
  }

  void setConcurrency(int value) {
    _concurrency = value.clamp(minConcurrency, maxConcurrency);
    state = [...state];
    _startNext();
  }

  Future<void> cancel(String id) async {
    // Task 5 实现
  }

  void retry(String id) {
    // Task 5 实现
  }

  Future<void> openFolder(String id) async {
    // Task 5 实现
  }

  void _startNext() {
    var running = runningCount;
    while (running < _concurrency) {
      final next = _nextQueued();
      if (next == null) break;
      _begin(next);
      running++;
    }
  }

  DownloadTask? _nextQueued() {
    for (final t in state) {
      if (t.status == TaskStatus.queued) return t;
    }
    return null;
  }

  void _begin(DownloadTask task) {
    _patch(task.id,
        expected: const {TaskStatus.queued},
        status: TaskStatus.downloading);
    unawaited(_run(task));
  }

  Future<void> _run(DownloadTask task) async {
    final dir = await ref.read(downloadsDirProvider)();
    try {
      final path = await _service.download(
        DownloadRequest(
          url: task.url,
          preset: task.preset,
          outputDir: dir,
          taskId: task.id,
        ),
        onProgress: (p) => _patch(task.id,
            expected: const {TaskStatus.downloading}, progress: p),
      );
      _patch(task.id,
          expected: const {TaskStatus.downloading, TaskStatus.canceling},
          status: TaskStatus.completed,
          filePath: path);
    } catch (e) {
      final wasCanceling =
          _find(task.id)?.status == TaskStatus.canceling;
      _patch(task.id,
          expected: const {TaskStatus.downloading, TaskStatus.canceling},
          status: wasCanceling ? TaskStatus.canceled : TaskStatus.failed,
          errorSummary:
              wasCanceling ? null : _friendly(e));
    } finally {
      scheduleMicrotask(_startNext);
    }
  }

  String _friendly(Object e) =>
      e is DownloadException ? e.friendlyMessage : '发生意外错误，请重试';

  DownloadTask? _find(String id) {
    for (final t in state) {
      if (t.id == id) return t;
    }
    return null;
  }

  void _upsert(DownloadTask task) {
    state = [...state, task];
  }

  void _patch(
    String id, {
    Set<TaskStatus>? expected,
    TaskStatus? status,
    DownloadProgress? progress,
    String? filePath,
    String? errorSummary,
  }) {
    final current = _find(id);
    if (current == null) return;
    if (expected != null && !expected.contains(current.status)) return;
    state = [
      for (final t in state)
        if (t.id == id)
          t.copyWith(
              status: status,
              progress: progress,
              filePath: filePath,
              errorSummary: errorSummary)
        else
          t,
    ];
  }
}
```

注意：Task 5 实施后目录解析仅存在于 `_run`（enqueue 的预解析已被裁裁定移除——目录失败应落为 failed 任务而非 enqueue 抛异常）。

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `flutter test test/features/download/queue_controller_test.dart`（4/4）→ `flutter test` → `flutter analyze`
Expected: 全部通过

- [ ] **Step 5: 提交**

```powershell
git add lib/features/download/providers.dart lib/features/download/queue_controller.dart test/features/download/queue_controller_test.dart
git commit -m "feat(queue): riverpod queue with concurrency and progress"
```

---

### Task 5: 队列控制器 —— 取消 / 重试 / 打开文件夹

**Files:**
- Modify: `lib/features/download/queue_controller.dart`（填充三个占位方法）
- Test: `test/features/download/queue_controller_test.dart`（追加用例）

**Interfaces:**
- Consumes: Task 2 的 `service.cancel(taskId)`（进程树终止）；Task 4 的 `_patch` expected 防御
- Produces:
  - `cancel(id)`：queued → 直接 canceled（不调 service）；downloading → 先置 canceling 再 `await _service.cancel(id)`；canceling/终态 → 无操作
  - `retry(id)`：仅 failed/canceled → 重置为 queued（清 progress/filePath/error），任务 id 不变，立即尝试启动
  - `openFolder(id)`：completed 且有 filePath → `Process.run('explorer.exe', ['/select,${filePath}'])`

- [ ] **Step 1: 追加失败测试**

```dart
  test('cancel queued task transitions directly to canceled', () async {
    // 占满唯一并发槽（默认并发 3 → 先调低到 1），第二个任务保持排队
    await controller.setConcurrency(1);
    await controller.enqueue(url: 'u0', title: '视频0', preset: QualityPreset.best);
    await _settle();
    final id2 = await controller.enqueue(url: 'u1', title: '视频1', preset: QualityPreset.best);
    await _settle();

    await controller.cancel(id2);
    await _settle();

    final tasks = container.read(downloadQueueProvider);
    expect(tasks[1].status, TaskStatus.canceled);
    expect(fake.canceled, isEmpty); // 排队任务不触碰 service
  });

  test('cancel downloading task kills tree and lands canceled', () async {
    final id = await controller.enqueue(
        url: 'u0', title: '视频0', preset: QualityPreset.best);
    await _settle();

    await controller.cancel(id);
    await _settle();

    expect(fake.canceled, ['t0']);
    final tasks = container.read(downloadQueueProvider);
    expect(tasks.single.status, TaskStatus.canceled);
  });

  test('canceling then late failure lands canceled without error text',
      () async {
    final id = await controller.enqueue(
        url: 'u0', title: '视频0', preset: QualityPreset.best);
    await _settle();
    // 不等 cancel 完成（fake.cancel 不结束 completer），
    // 直接模拟 kill 后的下载失败回调
    unawaited(controller.cancel(id));
    fake.pending[id]?.completeError(const DownloadException('下载失败：未知错误'));
    await _settle();
    await _settle();

    final tasks = container.read(downloadQueueProvider);
    expect(tasks.single.status, TaskStatus.canceled);
    expect(tasks.single.errorSummary, isNull);
  });

  test('retry failed task resets and reruns with same id', () async {
    final id = await controller.enqueue(
        url: 'u0', title: '视频0', preset: QualityPreset.best);
    await _settle();
    fake.pending[id]!.completeError(const DownloadException('网络错误：请检查网络连接后重试'));
    await _settle();

    controller.retry(id);
    await _settle();

    final tasks = container.read(downloadQueueProvider);
    expect(tasks.single.id, 't0');
    expect(tasks.single.status, TaskStatus.downloading);
    expect(fake.started.length, 2);
  });

  test('setConcurrency raises effective parallelism', () async {
    await controller.setConcurrency(2);
    await controller.enqueue(url: 'u0', title: '视频0', preset: QualityPreset.best);
    await controller.enqueue(url: 'u1', title: '视频1', preset: QualityPreset.best);
    await _settle();

    final statuses = container
        .read(downloadQueueProvider)
        .map((t) => t.status)
        .toList();
    expect(statuses, everyElement(TaskStatus.downloading));
    expect(controller.concurrency, 2);
  });
```

文件顶部需补 `import 'dart:async';`（已有）与 `unawaited`（dart:async 提供）。注意第一个用例里误写的 `setConcurrencyForTest` 行**不要保留**——以最终版测试代码为准（下方实现后统一核对：删除该行，仅靠占满并发槽使第二个任务排队）。

- [ ] **Step 2: 运行确认失败**

Run: `flutter test test/features/download/queue_controller_test.dart`
Expected: FAIL（cancel/retry 为空实现，状态不流转；setConcurrency 已有）

- [ ] **Step 3: 实现（替换三个占位方法）**

```dart
  Future<void> cancel(String id) async {
    final task = _find(id);
    if (task == null) return;
    if (task.status == TaskStatus.queued) {
      _patch(id,
          expected: const {TaskStatus.queued},
          status: TaskStatus.canceled);
      return;
    }
    if (task.status == TaskStatus.downloading) {
      _patch(id,
          expected: const {TaskStatus.downloading},
          status: TaskStatus.canceling);
      await _service.cancel(id);
    }
  }

  void retry(String id) {
    final task = _find(id);
    if (task == null) return;
    if (task.status != TaskStatus.failed &&
        task.status != TaskStatus.canceled) {
      return;
    }
    _patch(id,
        expected: const {TaskStatus.failed, TaskStatus.canceled},
        status: TaskStatus.queued,
        clearProgress: true,
        clearFilePath: true,
        clearError: true);
    _startNext();
  }

  Future<void> openFolder(String id) async {
    final task = _find(id);
    final path = task?.filePath;
    if (task == null || path == null || task.status != TaskStatus.completed) {
      return;
    }
    await Process.run('explorer.exe', ['/select,$path']);
  }
```

同时给 `_patch` 增加 clear 透传：签名追加 `bool clearProgress = false, bool clearFilePath = false, bool clearError = false`，构造 copyWith 时传入。

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `flutter test test/features/download/queue_controller_test.dart`（9/9）→ `flutter test` → `flutter analyze`
Expected: 全部通过；核对第一个用例无 `setConcurrencyForTest` 残留

- [ ] **Step 5: 提交**

```powershell
git add lib/features/download/queue_controller.dart test/features/download/queue_controller_test.dart
git commit -m "feat(queue): cancel with tree-kill, retry, open folder"
```

---

### Task 6: 分析控制器

**Files:**
- Create: `lib/features/download/analysis_controller.dart`
- Modify: `lib/features/download/providers.dart`（追加 analysisProvider 两行）
- Test: `test/features/download/analysis_controller_test.dart`

**Interfaces:**
- Consumes: `ytDlpServiceProvider`、`YtDlpService.probe`
- Produces:
  - `sealed class AnalysisState`：`AnalysisIdle` / `AnalysisLoading` / `AnalysisVideo(meta)` / `AnalysisPlaylist(meta)` / `AnalysisError(message)`（均 const 构造）
  - `AnalysisController extends Notifier<AnalysisState>`：`Future<void> analyze(String url)`（Loading → probe → Video/Playlist/Error；DownloadException → friendlyMessage；其他异常 → '发生意外错误，请重试'）、`void reset()`（回 Idle）

- [ ] **Step 1: 写失败测试**

```dart
// test/features/download/analysis_controller_test.dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:video_downloader/engine/models.dart';
import 'package:video_downloader/engine/yt_dlp_service.dart';
import 'package:video_downloader/features/download/analysis_controller.dart';
import 'package:video_downloader/features/download/providers.dart';

class FakeProbeService extends YtDlpService {
  FakeProbeService({this.result, this.error, this.generic = false});
  final AnalysisResult? result;
  final DownloadException? error;
  final bool generic;

  @override
  Future<AnalysisResult> probe(String url) async {
    if (generic) throw const SocketLike();
    if (error != null) throw error!;
    return result!;
  }
}

class SocketLike implements Exception {
  const SocketLike();
}

void main() {
  ProviderContainer makeContainer(YtDlpService service) {
    final c = ProviderContainer(overrides: [
      ytDlpServiceProvider.overrideWithValue(service),
    ]);
    addTearDown(c.dispose);
    return c;
  }

  test('idle by default and reset returns to idle', () {
    final c = makeContainer(FakeProbeService(null));
    expect(c.read(analysisProvider), isA<AnalysisIdle>());
    c.read(analysisProvider.notifier).reset();
    expect(c.read(analysisProvider), isA<AnalysisIdle>());
  });

  test('analyze video lands in AnalysisVideo', () async {
    final c = makeContainer(FakeProbeService(VideoResult(
        const VideoMeta(id: '1', title: '测试视频', webUrl: 'u'))));
    await c.read(analysisProvider.notifier).analyze('u');
    final s = c.read(analysisProvider);
    expect(s, isA<AnalysisVideo>());
    expect((s as AnalysisVideo).meta.title, '测试视频');
  });

  test('analyze playlist lands in AnalysisPlaylist', () async {
    final c = makeContainer(FakeProbeService(PlaylistResult(
        const PlaylistMeta(title: 'PL', entries: []))));
    await c.read(analysisProvider.notifier).analyze('u');
    expect(c.read(analysisProvider), isA<AnalysisPlaylist>());
  });

  test('download exception surfaces friendly message', () async {
    final c = makeContainer(FakeProbeService(
        error: const DownloadException('网络错误：请检查网络连接后重试')));
    await c.read(analysisProvider.notifier).analyze('u');
    final s = c.read(analysisProvider);
    expect(s, isA<AnalysisError>());
    expect((s as AnalysisError).message, contains('网络'));
  });

  test('generic exception surfaces generic message', () async {
    final c = makeContainer(FakeProbeService(generic: true));
    await c.read(analysisProvider.notifier).analyze('u');
    expect(
        (c.read(analysisProvider) as AnalysisError).message, contains('意外'));
  });
}
```

- [ ] **Step 2: 运行确认失败**

Run: `flutter test test/features/download/analysis_controller_test.dart`
Expected: FAIL（analysis_controller.dart 不存在；providers.dart 缺 analysisProvider）

- [ ] **Step 3: 实现**

```dart
// lib/features/download/analysis_controller.dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:video_downloader/engine/models.dart';
import 'package:video_downloader/features/download/providers.dart';

sealed class AnalysisState {
  const AnalysisState();
}

class AnalysisIdle extends AnalysisState {
  const AnalysisIdle();
}

class AnalysisLoading extends AnalysisState {
  const AnalysisLoading();
}

class AnalysisVideo extends AnalysisState {
  const AnalysisVideo(this.meta);
  final VideoMeta meta;
}

class AnalysisPlaylist extends AnalysisState {
  const AnalysisPlaylist(this.meta);
  final PlaylistMeta meta;
}

class AnalysisError extends AnalysisState {
  const AnalysisError(this.message);
  final String message;
}

class AnalysisController extends Notifier<AnalysisState> {
  @override
  AnalysisState build() {
    ref.watch(ytDlpServiceProvider);
    return const AnalysisIdle();
  }

  Future<void> analyze(String url) async {
    state = const AnalysisLoading();
    try {
      final result = await ref.read(ytDlpServiceProvider).probe(url);
      state = switch (result) {
        VideoResult(:final meta) => AnalysisVideo(meta),
        PlaylistResult(:final meta) => AnalysisPlaylist(meta),
      };
    } on DownloadException catch (e) {
      state = AnalysisError(e.friendlyMessage);
    } catch (_) {
      state = const AnalysisError('发生意外错误，请重试');
    }
  }

  void reset() => state = const AnalysisIdle();
}
```

providers.dart 追加：

```dart
final analysisProvider =
    NotifierProvider<AnalysisController, AnalysisState>(AnalysisController.new);
```

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `flutter test test/features/download/analysis_controller_test.dart`（5/5）→ `flutter test` → `flutter analyze`

- [ ] **Step 5: 提交**

```powershell
git add lib/features/download/analysis_controller.dart lib/features/download/providers.dart test/features/download/analysis_controller_test.dart
git commit -m "feat(analysis): riverpod analysis state machine"
```

---

### Task 7: fluent_ui Shell 与下载页迁移

**Files:**
- Create: `lib/features/shell/app_shell.dart`（NavigationView + PlaceholderPage）
- Create: `lib/features/download/download_page.dart`（M1 页面迁移 + 队列 UI）
- Modify: `lib/main.dart`（FluentApp + ProviderScope，删除 M1 页面代码）
- Rewrite: `test/ui/download_page_test.dart`（Riverpod overrides 版）
- Create: `test/ui/app_shell_test.dart`

**Interfaces:**
- Consumes: Task 4/5/6 的 providers 与控制器；`QualityPreset.label`
- Produces:
  - `AppShell`：NavigationView，三窗格（下载 FluentIcons.download / 历史 FluentIcons.history / 设置 FluentIcons.settings），历史与设置为 `PlaceholderPage(message)`（'历史记录将在下个版本提供' / '设置将在下个版本提供'）
  - `DownloadPage`（ConsumerStatefulWidget）：分析行（TextBox `url_field` + `analyze_button`）→ AnalysisState 分支渲染（Loading=ProgressBar；Error/Playlist=InfoBar；Video=Card 含缩略图、标题、上传者、时长、`ComboBox<QualityPreset>`、`enqueue_button`）→ 任务列表（每行 Card：标题、状态行、`cancel_<id>` / `retry_<id>` / `open_<id>` 按钮，key 规则照此）
  - 状态行文案：排队中 / 百分比+速度+剩余 / 正在取消... / 完成：路径 / errorSummary / 已取消
  - main.dart：`runApp(const ProviderScope(child: App()))`；`App` = `FluentApp(title: 'Video Downloader', theme: FluentThemeData(accentColor: Colors.indigo), home: AppShell())`
  - M1 的 main.dart 页面代码与 `EngineLocator` UI 依赖删除（ffmpeg 由 service 回退，落实最终审查建议）；post-await setState 全部加 `if (!mounted) return;`

- [ ] **Step 1: 写失败测试（重写 test/ui/download_page_test.dart + 新建 app_shell_test.dart）**

```dart
// test/ui/download_page_test.dart
import 'dart:async';

import 'package:fluent_ui/fluent_ui.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:video_downloader/engine/models.dart';
import 'package:video_downloader/engine/yt_dlp_service.dart';
import 'package:video_downloader/features/download/providers.dart';
import 'package:video_downloader/features/download/download_page.dart';
import 'package:video_downloader/features/shell/app_shell.dart';

class FakeUiService extends YtDlpService {
  FakeUiService({this.playlist = false, this.pendingDownload = false});
  final bool playlist;
  final bool pendingDownload;
  final Map<String, Completer<String>> pending = {};

  @override
  Future<AnalysisResult> probe(String url) async {
    if (playlist) {
      return PlaylistResult(const PlaylistMeta(title: 'PL', entries: [
        PlaylistEntry(id: 'a', title: 'A', url: 'u1'),
      ]));
    }
    return VideoResult(const VideoMeta(
        id: '1', title: '测试视频', uploader: 'UP主', durationSec: 61,
        webUrl: url));
  }

  @override
  Future<String> download(DownloadRequest request,
      {void Function(DownloadProgress)? onProgress}) async {
    if (pendingDownload) {
      final c = Completer<String>();
      pending[request.taskId!] = c;
      onProgress?.call(const DownloadProgress(
          fraction: 0.4, speed: '1MiB/s', etaSeconds: 30));
      return c.future;
    }
    onProgress?.call(const DownloadProgress(fraction: 1.0));
    return 'C:\\tmp\\测试视频.mp4';
  }

  @override
  Future<void> cancel(String taskId) async {
    // 必须覆盖：模拟真实 kill 后下载以非零码结束，
    // 否则挂起的 download future 永不完成，任务卡在 canceling
    pending.remove(taskId)?.completeError(const DownloadException('已取消'));
  }
}

Widget wrap(FakeUiService service) => ProviderScope(
      overrides: [
        ytDlpServiceProvider.overrideWithValue(service),
        downloadsDirProvider.overrideWithValue(() async => 'C:\\tmp'),
      ],
      child: FluentApp(home: AppShell()),
    );

void main() {
  testWidgets('analyze then enqueue lands a completed task', (tester) async {
    await tester.pumpWidget(wrap(FakeUiService()));
    await tester.enterText(
        find.byKey(const Key('url_field')), 'https://example.com/v');
    await tester.tap(find.byKey(const Key('analyze_button')));
    await tester.pumpAndSettle();
    expect(find.textContaining('测试视频'), findsWidgets);

    await tester.tap(find.byKey(const Key('enqueue_button')));
    await tester.pumpAndSettle();
    expect(find.textContaining('完成：C:\\tmp\\测试视频.mp4'), findsOneWidget);
    expect(find.byKey(const Key('open_t0')), findsOneWidget);
  });

  testWidgets('playlist url shows friendly notice', (tester) async {
    await tester.pumpWidget(wrap(FakeUiService(playlist: true)));
    await tester.enterText(
        find.byKey(const Key('url_field')), 'https://example.com/pl');
    await tester.tap(find.byKey(const Key('analyze_button')));
    await tester.pumpAndSettle();
    expect(find.textContaining('批量下载将在下个版本提供'), findsOneWidget);
    expect(find.byKey(const Key('enqueue_button')), findsNothing);
  });

  testWidgets('cancel downloads task lands canceled', (tester) async {
    await tester.pumpWidget(wrap(FakeUiService(pendingDownload: true)));
    await tester.enterText(
        find.byKey(const Key('url_field')), 'https://example.com/v');
    await tester.tap(find.byKey(const Key('analyze_button')));
    await tester.pumpAndSettle();
    await tester.tap(find.byKey(const Key('enqueue_button')));
    await tester.pumpAndSettle();
    expect(find.textContaining('40.0%'), findsOneWidget);

    await tester.tap(find.byKey(const Key('cancel_t0')));
    await tester.pumpAndSettle();
    expect(find.text('已取消'), findsOneWidget);
    expect(find.byKey(const Key('retry_t0')), findsOneWidget);
  });
}
```

```dart
// test/ui/app_shell_test.dart
import 'package:fluent_ui/fluent_ui.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:video_downloader/features/shell/app_shell.dart';
import 'download_page_test.dart' show FakeUiService;

void main() {
  testWidgets('nav switches between pages', (tester) async {
    await tester.pumpWidget(ProviderScope(
      overrides: [
        ytDlpServiceProvider.overrideWithValue(FakeUiService()),
      ],
      child: FluentApp(home: AppShell()),
    ));
    await tester.pumpAndSettle();
    expect(find.byKey(const Key('url_field')), findsOneWidget);

    await tester.tap(find.text('历史'));
    await tester.pumpAndSettle();
    expect(find.textContaining('历史记录将在下个版本提供'), findsOneWidget);

    await tester.tap(find.text('设置'));
    await tester.pumpAndSettle();
    expect(find.textContaining('设置将在下个版本提供'), findsOneWidget);
  });
}
```

- [ ] **Step 2: 运行确认失败**

Run: `flutter test test/ui/download_page_test.dart` 与 `flutter test test/ui/app_shell_test.dart`
Expected: FAIL（download_page.dart / app_shell.dart 不存在）

- [ ] **Step 3: 实现**

```dart
// lib/features/shell/app_shell.dart
import 'package:fluent_ui/fluent_ui.dart';
import 'package:video_downloader/features/download/download_page.dart';

class AppShell extends StatefulWidget {
  const AppShell({super.key});

  @override
  State<AppShell> createState() => _AppShellState();
}

class _AppShellState extends State<AppShell> {
  int _index = 0;

  @override
  Widget build(BuildContext context) {
    return NavigationView(
      appBar: const NavigationAppBar(title: Text('Video Downloader')),
      pane: NavigationPane(
        selected: _index,
        onChanged: (i) => setState(() => _index = i),
        items: [
          PaneItem(
            icon: const Icon(FluentIcons.download),
            title: const Text('下载'),
            body: const DownloadPage(),
          ),
          PaneItem(
            icon: const Icon(FluentIcons.history),
            title: const Text('历史'),
            body: const PlaceholderPage(message: '历史记录将在下个版本提供'),
          ),
          PaneItem(
            icon: const Icon(FluentIcons.settings),
            title: const Text('设置'),
            body: const PlaceholderPage(message: '设置将在下个版本提供'),
          ),
        ],
      ),
    );
  }
}

class PlaceholderPage extends StatelessWidget {
  const PlaceholderPage({super.key, required this.message});
  final String message;

  @override
  Widget build(BuildContext context) {
    return ScaffoldPage(
      content: Center(child: Text(message)),
    );
  }
}
```

```dart
// lib/features/download/download_page.dart
import 'package:fluent_ui/fluent_ui.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:video_downloader/engine/models.dart';
import 'package:video_downloader/features/download/analysis_controller.dart';
import 'package:video_downloader/features/download/download_task.dart';
import 'package:video_downloader/features/download/providers.dart';
import 'package:video_downloader/features/download/queue_controller.dart';

class DownloadPage extends ConsumerStatefulWidget {
  const DownloadPage({super.key});

  @override
  ConsumerState<DownloadPage> createState() => _DownloadPageState();
}

class _DownloadPageState extends ConsumerState<DownloadPage> {
  final _urlCtrl = TextEditingController();
  QualityPreset _preset = QualityPreset.best;

  Future<void> _analyze() async {
    final url = _urlCtrl.text.trim();
    if (url.isEmpty) return;
    await ref.read(analysisProvider.notifier).analyze(url);
    if (!mounted) return;
    setState(() {});
  }

  Future<void> _enqueue() async {
    final analysis = ref.read(analysisProvider);
    if (analysis is! AnalysisVideo) return;
    final video = analysis.meta;
    final url = video.webUrl.isNotEmpty ? video.webUrl : _urlCtrl.text.trim();
    await ref.read(downloadQueueProvider.notifier).enqueue(
        url: url, title: video.title, preset: _preset);
    if (!mounted) return;
    ref.read(analysisProvider.notifier).reset();
    _urlCtrl.clear();
  }

  @override
  Widget build(BuildContext context) {
    final analysis = ref.watch(analysisProvider);
    final tasks = ref.watch(downloadQueueProvider);
    final analyzing = analysis is AnalysisLoading;
    return ScaffoldPage(
      content: Padding(
        padding: const EdgeInsets.all(16),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.stretch,
          children: [
            Row(children: [
              Expanded(
                child: TextBox(
                  key: const Key('url_field'),
                  controller: _urlCtrl,
                  placeholder: '粘贴视频链接',
                ),
              ),
              const SizedBox(width: 8),
              FilledButton(
                key: const Key('analyze_button'),
                onPressed: analyzing ? null : _analyze,
                child: Text(analyzing ? '分析中...' : '分析'),
              ),
            ]),
            const SizedBox(height: 12),
            _analysisView(analysis),
            const SizedBox(height: 12),
            const Align(
              alignment: Alignment.centerLeft,
              child: Text('任务列表'),
            ),
            Expanded(
              child: ListView.builder(
                itemCount: tasks.length,
                itemBuilder: (context, i) => _taskRow(tasks[i]),
              ),
            ),
          ],
        ),
      ),
    );
  }

  Widget _analysisView(AnalysisState analysis) {
    if (analysis is AnalysisIdle) return const SizedBox.shrink();
    if (analysis is AnalysisLoading) return const ProgressBar();
    if (analysis is AnalysisError) {
      return InfoBar(
          severity: InfoBarSeverity.warning,
          title: Text(analysis.message));
    }
    if (analysis is AnalysisPlaylist) {
      return InfoBar(
          severity: InfoBarSeverity.info,
          title: Text(
              '检测到播放列表「${analysis.meta.title ?? ''}」，共 ${analysis.meta.entries.length} 个条目；批量下载将在下个版本提供'));
    }
    final video = (analysis as AnalysisVideo).meta;
    return Card(
      child: Row(children: [
        SizedBox(
          width: 120,
          height: 68,
          child: video.thumbnailUrl == null
              ? const Center(child: Icon(FluentIcons.video))
              : Image.network(video.thumbnailUrl!, fit: BoxFit.cover,
                  errorBuilder: (_, __, ___) =>
                      const Center(child: Icon(FluentIcons.video))),
        ),
        const SizedBox(width: 12),
        Expanded(
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Text(video.title, maxLines: 1, overflow: TextOverflow.ellipsis),
              Text('${video.uploader ?? '未知上传者'} · ${video.durationSec ?? 0} 秒'),
            ],
          ),
        ),
        const SizedBox(width: 8),
        ComboBox<QualityPreset>(
          value: _preset,
          items: [
            for (final p in QualityPreset.values)
              ComboBoxItem(value: p, child: Text(p.label)),
          ],
          onChanged: (v) => setState(() => _preset = v ?? _preset),
        ),
        const SizedBox(width: 8),
        FilledButton(
          key: const Key('enqueue_button'),
          onPressed: _enqueue,
          child: const Text('加入下载'),
        ),
      ]),
    );
  }

  Widget _taskRow(DownloadTask t) {
    final canCancel =
        t.status == TaskStatus.queued || t.status == TaskStatus.downloading;
    final canRetry =
        t.status == TaskStatus.failed || t.status == TaskStatus.canceled;
    return Card(
      child: Row(children: [
        Expanded(
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Text(t.title, maxLines: 1, overflow: TextOverflow.ellipsis),
              const SizedBox(height: 4),
              _statusLine(t),
            ],
          ),
        ),
        if (canCancel)
          Button(
            key: Key('cancel_${t.id}'),
            onPressed: () =>
                unawaited(ref.read(downloadQueueProvider.notifier).cancel(t.id)),
            child: const Text('取消'),
          ),
        if (t.status == TaskStatus.canceling)
          const Padding(
            padding: EdgeInsets.only(left: 8),
            child: Text('正在取消...'),
          ),
        if (canRetry)
          Button(
            key: Key('retry_${t.id}'),
            onPressed: () => ref.read(downloadQueueProvider.notifier).retry(t.id),
            child: const Text('重试'),
          ),
        if (t.status == TaskStatus.completed)
          Button(
            key: Key('open_${t.id}'),
            onPressed: () => unawaited(
                ref.read(downloadQueueProvider.notifier).openFolder(t.id)),
            child: const Text('打开文件夹'),
          ),
      ]),
    );
  }

  Widget _statusLine(DownloadTask t) {
    switch (t.status) {
      case TaskStatus.queued:
        return const Text('排队中');
      case TaskStatus.downloading:
        final p = t.progress;
        return Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            ProgressBar(value: (p?.fraction ?? 0) * 100),
            const SizedBox(height: 2),
            Text(p == null
                ? '下载中'
                : '${(p.fraction * 100).toStringAsFixed(1)}%  ${p.speed ?? ''}'
                    '${p.etaSeconds != null ? '  剩余 ${p.etaSeconds}s' : ''}'),
          ],
        );
      case TaskStatus.canceling:
        return const Text('正在取消...');
      case TaskStatus.completed:
        return Text('完成：${t.filePath ?? ''}');
      case TaskStatus.failed:
        return Text(t.errorSummary ?? '下载失败');
      case TaskStatus.canceled:
        return const Text('已取消');
    }
  }

  @override
  void dispose() {
    _urlCtrl.dispose();
    super.dispose();
  }
}
```

（`unawaited` 来自 dart:async——download_page.dart 顶部补 `import 'dart:async';`。）

main.dart 整体重写：

```dart
// lib/main.dart
import 'package:fluent_ui/fluent_ui.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:video_downloader/features/shell/app_shell.dart';

void main() {
  runApp(const ProviderScope(child: App()));
}

class App extends StatelessWidget {
  const App({super.key});

  @override
  Widget build(BuildContext context) {
    return FluentApp(
      title: 'Video Downloader',
      theme: const FluentThemeData(accentColor: Colors.indigo),
      home: AppShell(),
    );
  }
}
```

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `flutter test test/ui/download_page_test.dart`（3/3）→ `flutter test test/ui/app_shell_test.dart`（1/1）→ `flutter test` → `flutter analyze`
Expected: 全部通过（fluent_ui 的动画如致 pumpAndSettle 偶发超时，可将该处断言改为固定次数 `tester.pump(const Duration(milliseconds: 300))` 并在报告中注明）

- [ ] **Step 5: 提交**

```powershell
git add lib/features/shell/app_shell.dart lib/features/download/download_page.dart lib/main.dart test/ui/download_page_test.dart test/ui/app_shell_test.dart
git commit -m "feat(ui): fluent_ui shell with queue page, cancel and retry"
```

---

### Task 8: 端到端验证与 GUI 冒烟

**Files:**
- Create: `test/e2e/queue_e2e_test.dart`（真实引擎端到端，**随仓库提交**，默认跳过）
- Create: `dart_test.yaml`（e2e tag 默认 skip，普通 `flutter test` 不触网）
- Modify: 无其他

**Interfaces:**
- Consumes: Task 4/5 的队列控制器 + `ProviderContainer`；真实 `YtDlpService`（`YTDLP_ENGINE_DIR` 环境变量定位引擎——M1 EngineLocator 的 env 通道在此发挥作用）
- Produces: M2 出口标准——真实队列：串行并发、取消（进程树终止）、重试全部在真实引擎上验证
- **车辆说明：队列层依赖 flutter_riverpod（Flutter SDK），不能用 `dart run` 执行；正确车辆是 `flutter test --tags e2e`（Dart VM 上运行，dart:io 可用，可拉起真实进程）**

- [ ] **Step 1: 确保引擎就位并重建**

worktree 是全新检出（无引擎二进制）：

```powershell
powershell -ExecutionPolicy Bypass -File tool\fetch_engine.ps1
& "D:\Application\DevTools\flutter\flutter3.44.9\bin\flutter.bat" build windows --debug
```

Expected: assets/bin 下三文件就位；构建成功

- [ ] **Step 2: 写 E2E 测试与 tag 配置**

`dart_test.yaml`（downloadApp 根目录）：

```yaml
tags:
  e2e:
    skip: "requires real network and bundled engine; run explicitly with --tags e2e"
```

```dart
// test/e2e/queue_e2e_test.dart
@Tags(['e2e'])
import 'dart:io';

import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:video_downloader/features/download/download_task.dart';
import 'package:video_downloader/features/download/providers.dart';
import 'package:video_downloader/features/download/queue_controller.dart';

const _bili = 'https://www.bilibili.com/video/BV1GJ411x7h7';
const _yt = 'https://www.youtube.com/watch?v=jNQXAC9IVRw';

Future<TaskStatus> waitStatus(ProviderContainer c, String id,
    Set<TaskStatus> wanted, Duration limit) async {
  final end = DateTime.now().add(limit);
  while (DateTime.now().isBefore(end)) {
    final matches = c
        .read(downloadQueueProvider)
        .where((t) => t.id == id)
        .toList();
    if (matches.isNotEmpty && wanted.contains(matches.single.status)) {
      return matches.single.status;
    }
    await Future<void>.delayed(const Duration(milliseconds: 300));
  }
  fail('timeout waiting $wanted for task $id');
}

void main() {
  late ProviderContainer container;
  late Directory tmp;

  setUp(() async {
    tmp = await Directory.systemTemp.createTemp('e2e_m2');
    container = ProviderContainer(overrides: [
      downloadsDirProvider.overrideWithValue(() async => tmp.path),
    ]);
  });

  tearDown(() async {
    container.dispose();
    await tmp.delete(recursive: true);
  });

  test('serial queue completes tasks one at a time (concurrency=1)',
      () async {
    final controller = container.read(downloadQueueProvider.notifier);
    controller.setConcurrency(1);
    final a = await controller.enqueue(
        url: _bili, title: 'serial-A', preset: QualityPreset.p480);
    final b = await controller.enqueue(
        url: _yt, title: 'serial-B', preset: QualityPreset.best);

    final earlyB = await waitStatus(container, b,
        {TaskStatus.queued, TaskStatus.downloading}, const Duration(minutes: 1));
    if (earlyB == TaskStatus.queued) {
      // A 占用唯一并发槽期间 B 保持排队，且同时只有 1 个任务在下载
      expect(
          container
              .read(downloadQueueProvider)
              .where((t) => t.status == TaskStatus.downloading)
              .length,
          1);
    }

    expect(
        await waitStatus(container, a,
            {TaskStatus.completed, TaskStatus.failed}, const Duration(minutes: 8)),
        TaskStatus.completed,
        reason: 'A error: ${container.read(downloadQueueProvider).firstWhere((t) => t.id == a).errorSummary}');
    expect(
        await waitStatus(container, b,
            {TaskStatus.completed, TaskStatus.failed}, const Duration(minutes: 8)),
        TaskStatus.completed,
        reason: 'B error: ${container.read(downloadQueueProvider).firstWhere((t) => t.id == b).errorSummary}');
  }, timeout: const Timeout(Duration(minutes: 10)));

  test('cancel kills the process tree and lands canceled', () async {
    final controller = container.read(downloadQueueProvider.notifier);
    controller.setConcurrency(1);
    final c = await controller.enqueue(
        url: _bili, title: 'cancel-me', preset: QualityPreset.best);

    await waitStatus(container, c, {TaskStatus.downloading},
        const Duration(minutes: 2));
    await controller.cancel(c);
    expect(
        await waitStatus(container, c, {TaskStatus.canceled},
            const Duration(seconds: 30)),
        TaskStatus.canceled);

    // 无孤儿进程：taskkill /T 后不应残留 yt-dlp / ffmpeg
    final tasklist = await Process.run('tasklist', []);
    final out = tasklist.stdout as String;
    expect(out.contains('yt-dlp.exe'), isFalse, reason: out);
    expect(out.contains('ffmpeg.exe'), isFalse, reason: out);
  }, timeout: const Timeout(Duration(minutes: 5)));

  test('retry reruns a canceled task', () async {
    final controller = container.read(downloadQueueProvider.notifier);
    controller.setConcurrency(1);
    final c = await controller.enqueue(
        url: _bili, title: 'retry-me', preset: QualityPreset.best);
    await waitStatus(container, c, {TaskStatus.downloading},
        const Duration(minutes: 2));
    await controller.cancel(c);
    await waitStatus(container, c, {TaskStatus.canceled},
        const Duration(seconds: 30));

    controller.retry(c);
    expect(
        await waitStatus(container, c,
            {TaskStatus.downloading, TaskStatus.completed},
            const Duration(minutes: 2)),
        isNot(TaskStatus.canceled));
    // 收尾：再次取消，避免后台残留下载
    await controller.cancel(c);
    await waitStatus(container, c, {TaskStatus.canceled, TaskStatus.completed},
        const Duration(seconds: 30));
  }, timeout: const Timeout(Duration(minutes: 5)));
}
```

运行（需环境变量指向引擎目录）：

```powershell
$env:YTDLP_ENGINE_DIR = (Resolve-Path "assets\bin").Path
& "D:\Application\DevTools\flutter\flutter3.44.9\bin\flutter.bat" test --tags e2e
```

Expected: 3/3 通过。若站点阻断候选链接，换用其他短视频 URL 并在报告注明（不得伪造成功）。默认 `flutter test` 中 e2e 显示为 Skipped。

- [ ] **Step 3: 全量回归（确认 e2e 默认跳过）**

```powershell
& "D:\Application\DevTools\flutter\flutter3.44.9\bin\flutter.bat" test
& "D:\Application\DevTools\flutter\flutter3.44.9\bin\flutter.bat" analyze
```

Expected: 普通套件全绿且 e2e 被跳过；analyze 无问题

- [ ] **Step 4: 提交**

```powershell
git add test/e2e/queue_e2e_test.dart dart_test.yaml
git commit -m "test(e2e): real-engine queue e2e (serial, cancel, retry), skip-by-default tag"
```

- [ ] **Step 5: GUI 冒烟（与用户协作；先重建！）**

```powershell
& "D:\Application\DevTools\flutter\flutter3.44.9\bin\flutter.bat" build windows --debug
Start-Process "build\windows\x64\runner\Debug\video_downloader.exe"
```

用户核对清单：
1. 左侧导航三页可切换，历史/设置显示占位文案
2. 粘贴视频链接 → 分析 → 卡片出现（缩略图/标题/画质下拉）→ 加入下载
3. 连续加入 2–3 个任务 → 并发 3 内同时下载，进度条/速度/ETA 实时
4. 对一个下载中任务点取消 → 短暂“正在取消”后“已取消”→ 重试可重新下载
5. 失败任务（如需登录的视频）→ 友好错误文案 + 重试按钮
6. 完成任务 → 打开文件夹 → 资源管理器定位到文件
7. 全程无黑色控制台窗口闪现

- [ ] **Step 6: 若 Step 2–5 有修复，修复后全量回归并提交；无修复则确认工作树干净**

```powershell
git status --short   # 预期：干净
```

---

## 后续计划

- **Plan 3 (M3)**：播放列表批量 UI、drift 历史持久化（完成即落库，M2 的内存任务在关闭时移交给历史）、设置页（下载目录/并发/默认画质持久化）、intl 双语
- **Plan 4 (M4)**：引擎自动更新与自愈、错误打磨、免责声明、msix 打包（fetch 脚本加 SHA-256 校验）
